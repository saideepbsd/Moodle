<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saideep.moodleplus">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CoursesList" />
        <activity android:name=".GradesAll" />
        <activity android:name=".LogOut" />
        <activity android:name=".Notifications" />
        <activity android:name=".CourseContent" />
        <activity android:name=".CourseGrades" />
        <activity android:name=".ListofAssignments" />
        <activity android:name=".AssignmentInfo" />
        <activity android:name=".ThreadsList" />
        <activity android:name=".ThreadInfo"></activity>
    </application>

</manifest>
